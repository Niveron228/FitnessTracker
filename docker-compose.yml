version: '3.8'

services:
  api:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: my-fitness-app
    ports:
      - "8080:8080"
    environment:
      - ASPNETCORE_ENVIRONMENT=Development
    volumes:
      - ./:/app/data
